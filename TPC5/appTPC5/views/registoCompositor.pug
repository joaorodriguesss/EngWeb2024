extends layout

block content
	.w3-card-4
		header.w3-container.w3-blue 
			h1= titulo
		
		.w3-container
			br
			form.w3-continer(method= 'POST')
				fieldset
					legend Metadata
					label ID 
					input.w3-input.w3-round(type='text', name='id')
					label Nome
					input.w3-input.w3-round(type='text', name='nome')
					label Biografia
					input.w3-input.w3-round(type='textarea', name='bio')
					label Periodo 
					input.w3-input.w3-round(type='text', name='periodo')
					label Data de Nascimento
					input.w3-input.w3-round(type='date', name='dataNasc')
					label Data do Óbito
					input.w3-input.w3-round(type='date', name='dataObito')

				br
				button.w3-button.w3-blue(type='submit', method='POST', action='/compositores/registo') Registar
				br
				br

		footer.w3-container.w3-blue
			address Gerado por A100711 em #{data}
			a.w3-button.w3-blue(href='javascript:history.back()') Voltar
			br
